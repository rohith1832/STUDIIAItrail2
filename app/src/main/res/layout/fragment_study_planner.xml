<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/studyPlannerRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_blue_gradient"
    tools:context=".ui.StudyPlannerFragment">

    <!-- Title -->
    <TextView
        android:id="@+id/plannerTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Study Planner"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:textSize="24sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp" />

    <!-- Topics input -->
    <EditText
        android:id="@+id/inputTopics"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:hint="Enter topics (comma separated)"
        android:textColor="@android:color/white"
        android:background="@drawable/bg_search_rounded"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:inputType="text"
        android:textStyle="italic"
        app:layout_constraintTop_toBottomOf="@id/plannerTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp" />

    <!-- Daily study time input -->
    <EditText
        android:id="@+id/inputDailyTime"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:hint="Daily study time in minutes (e.g., 120)"
        android:textColor="@android:color/white"
        android:background="@drawable/bg_search_rounded"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:textStyle="italic"
        android:inputType="number"
        app:layout_constraintTop_toBottomOf="@id/inputTopics"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp" />

    <!-- Generate button -->
    <Button
        android:id="@+id/btnGenerate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Generate Timetable"
        android:textAllCaps="false"
        android:textSize="18sp"
        app:layout_constraintTop_toBottomOf="@id/inputDailyTime"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"
        android:textStyle="italic"
        android:backgroundTint="@color/gold_accent"
        android:textColor="@android:color/black"
        android:layout_marginBottom="12dp"/>

    <!-- RecyclerView to show timetable -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewTimetable"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/btnGenerate"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
